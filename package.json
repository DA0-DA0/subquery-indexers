{
  "name": "subquery-indexers",
  "version": "0.0.1",
  "description": "Indexers built with SubQuery to support DAO DAO frontend and API needs.",
  "repository": "https://github.com/DA0-DA0/subquery-indexers",
  "license": "BSD 3-Clause Clear",
  "private": true,
  "scripts": {
    "akashify": "ts-node ./packages/akashify/akashify.ts",
    "lint": "yarn workspaces foreach -ipv run lint",
    "format": "yarn workspaces foreach -ipv run format"
  },
  "workspaces": [
    "indexers/*",
    "packages/*"
  ],
  "packageManager": "yarn@3.2.3"
}
